# teaspill — Worklog

Append-only findings ledger. One entry per completed task: what was built, deviations, surprises, open questions. Read before starting dependent work.

---

## ▶ RESUME POINTER (main session — update after every group)

- **Completed & committed:** 8.0 self-init, G1–G9 (G9 = T6.1 defineAgent / T5.2 frontend SDK). NOTE: G8's T3.2 was cut off by a session limit and finished on resume; G9's T6.1 first agent aborted with no work and was re-dispatched fresh (both handled, all green).
- **NEXT GROUP: G10** — T6.2 CLI + dev loop (M, Opus, cli) · T6.3 conformance kit (M, Opus, NEW `packages/conformance`) · T7.0 spike distillation digest (S/M, Sonnet, → `references/casdk-spike-digest.md`). Disjoint. **T6.2 owns the register-before-gateway-up retry/backoff** (T6.1 left a single attempt) + the real ingress-client wiring for spawn_agent/send_message/workspace tools (T6.1's `httpToolContext` needs `ingressUrl`; `listChildren` is a catalog-read seam returning `[]`). T6.3 = reusable test harness (spawn→respond, parallel fan-out all child_finished, crash-mid-run resume, projection continuity through a streams restart, workspace exec survives agent-loop restart) run against a live stack — consumes defineAgent (T6.1) + CLI (T6.2). **T7.0 is the GATE for G11's T7.1/T7.2** — a dedicated agent distills `../electric` CASDK spike (`CLAUDE_AGENT_SDK_PLAN.md` + the uncommitted diff) into `references/casdk-spike-digest.md`; extract KNOWLEDGE not tasks (PLAN §8.2 hard rule). Much is already captured in `docs/casdk-mapping.md` (G2) — T7.0 deepens it with file-path pointers for T7.1/T7.2 deep dives.
- **Gates status:** Gate 1 ✅. Gate 2 ✅. Gate 3 ✅. Gate 4 ✅. No gates outstanding. (T7.0 digest must exist before G11.)
- **Open D-amendments (all accepted, non-contradictory):** A1–A9. No thread HALTED.
- **Action items / carry-forwards (resolve at the noted task):** (1) **T8.1 (G12):** flip dead-letter default so `archived` resurrects + build resurrection BEFORE idle auto-archive relied on (A8) + persist catalog `archived_snapshot` JSONB + persist a stream BYTE offset beside `snapshot_offset` (T5.2 needs it for cheap fast-join; today only the snapshot seq is stored → reducer scans from 0, correct but not cheap). (2) **T6.2 (this next group):** register retry/backoff; real ingress clients for tools; `listChildren` real impl; `onWake` is accepted but not loop-wired yet (T6.1 reserved it). (3) **T7.2 (G12):** CASDK interrupt inherits verb-only control event (A8); forward `ctx.signal` to long exec (T4.3 gap: WorkspaceClient.exec has no AbortSignal); replace the `claudeAgentSdk` selection stub (T6.1 `harness.ts` — `finalize()` assembles tools, wire `buildHarness` to the CASDK harness). (4) **schema cleanup (any M task):** addressing regex fns are now ported in gateway AND frontend-sdk actions.ts — promote to `@teaspill/schema`. (5) later: wire reconciler live handlers + affine-offset append (A9).
- **Build note:** `esbuild` approved; Restate SDK 1.16.2; **durable-streams client `@durable-streams/client@0.2.6`** (T2.2, matches :0.1.4 server — the version all stdout/stream emitters should use). `pnpm install` clean; full repo `pnpm -r typecheck test` green (11 pkgs, 273 tests + live-infra skips).
- **Carry-forward items (not blocking):** (i) T4.1 executor handler names use `fsRead` not `fs.read` — the dot-in-handler-name grammar was NOT verified in T2.0; the gateway `/api` name-map / T6.1 decides public spelling — verify before relying on dotted names. (ii) T1.2 addressing fns ported into gateway should migrate to `@teaspill/schema`. (iii) A6 open: epoch-bump reset offset for T5.3.
- **Commit policy:** NO Claude co-author trailer (AGENTS.md; CLAUDE.md symlinks to it). History rewritten at G4 to strip prior co-author trailers.
- **To continue in a fresh session:** read PLAN.md §8 + this pointer + `git log --oneline`, then dispatch the NEXT GROUP. Model map: L→Fable, M→Opus, S→Sonnet.

---

## Main session — 8.0 self-initialization (2026-07-16)

- Confirmed working dir is `teaspill/` with PLAN.md; was NOT a git repo → ran `git init`.
- Created `DECISIONS.md` (D1–D8 + `name: teaspill`), this `WORKLOG.md`, and `references/`.
- `../electric` sibling **present**. Confirmed the CASDK spike lives in the working tree:
  `git -C ../electric status --porcelain` shows added `CLAUDE_AGENT_SDK_PLAN.md` and a new
  `packages/agents-runtime/src/claude/` dir (claude-adapter, session-store, repair, precompaction,
  history-policy, messages, usage, tools, SESSION_FORMAT.md, etc.) plus modified pi-adapter/types/context-factory.
  So T7.0 has full raw material. Not modifying anything under `../electric`.
- **Model dispatch mapping (per Andrés, overrides §8.3 default):** L → Fable (`fable`), M → Opus (`opus`), S → Sonnet (`sonnet`). Subagent `model` param set accordingly.

### Open questions / degradations
- None blocking. Web access availability for T2.0 (Restate docs) to be checked when G2 dispatches — confirmed WORKING in G1 (T0.4 fetched live).

---

## G1 (2026-07-17)

### T0.3 — Repo scaffold
Built pnpm workspace: 9 packages under `packages/*` per PLAN §5, each with package.json (`@teaspill/<pkg>`), tsconfig.json + tsconfig.build.json (tsc-only, no bundler), vitest.config.ts, src/index.ts + src/index.test.ts. Root: strict tsconfig.base.json, ESLint flat config + Prettier, Vitest, Changesets (all packages private), `.github/workflows/ci.yml` (install→lint→typecheck→test). CLI package has `bin: teaspill` + shebang. Pinned typescript@6.0.3 (NOT 7.0.2 — typescript-eslint@8.64 caps TS <6.1.0; see DECISIONS tooling note). Verified for real: install, typecheck, test, build, lint, format:check all green (9/9 pkgs, 10 tests). Fixed two real bugs while verifying: (1) root vitest `projects` glob broke per-package `vitest run` → gave each package its own vitest.config.ts; (2) tsc emitted *.test.ts into dist/ → double-run → split build tsconfig to exclude tests. 66 files. No commit (main committed). Deviation: none. DECISIONS amendment: none (tooling note only).

### T0.4 — License verification (gate)
Checked Restate server (BSL 1.1 → Apache-2.0 in 4yr/release; Additional Use Grant permits self-host, bars only multi-tenant "Public Restate Platform Service"), Restate TS SDK (MIT), durable-streams client (MIT — PLAN guessed Apache, corrected) + Rust server (Apache-2.0). Web access worked fully. **R1 verdict: PROCEED**, no DBOS/Temporal fallback needed. Standing constraint recorded in DECISIONS: don't ever expose raw Restate registration to third-party devs in a multi-tenant hosted mode. Full detail in DECISIONS "License verification (T0.4)".

### T0.2 — Addressing & naming model (docs/addressing.md)
Scheme: entity url `/t/<tenant>/a/<type>/<id>` = `entities.url` pk = entityId everywhere; gateway short form `/a/<type>/<id>` expands to default tenant. Segments `^[a-z0-9][a-z0-9_-]*$` (tenant≤32/type≤48/id≤64). Instance id = lowercase ULID (time-sortable); caller-supplied ids allowed for deterministic/idempotent spawn (Restate get-or-create arbitrates; re-spawn = no-op reattach). Streams: timeline `/t/<tenant>/agents/<type>/<id>/timeline`, sibling `/deltas` (name reserved, framing TBD by T0.1/T5.1), workspace stdout `/t/<tenant>/workspaces/<name>/stdout`. Workspace key `<tenant>/<name>` (private default `a-<type>-<id>`). Restate: agent svc `agent.<type>` key `<id>`; `steer` key = full entity url; `workspace` key = `<tenant>/<name>`; `cron` key = `<name>`. **durable-streams constraints** (read from `../electric` durable-streams-rust source; root PROTOCOL.md absent in checkout): C1 stream name = HTTP path verbatim, slashes fine; C2 on-disk path encode keeps `[A-Za-z0-9._-]`, others→`+`, trunc120+`~id` — our charset stays clean; C3 must PUT-create before POST-append (404 else); **C4 idempotent producer = (Producer-Id, Epoch, Seq), Seq MUST start 0 gapless +1** → Producer-Id=entity url, Seq=canonical seq. Electric `where` = scalar-column equality with positional params → recommend `entities.tenant` column + normalized `entity_tags` (jsonb where awkward). Reference TS derivation fns embedded in doc, destined for `packages/schema`. → DECISIONS A1 (seq 0-based gapless, binds T0.1), A2 (entities.tenant + entity_tags → T1.3), A3 (Restate service naming → confirm T2.0).

## G2 (2026-07-17)

### T0.1 + T3.1 (+T7.1 paper-mapping) — Canonical event schema · Harness interface · CASDK mapping — **PROPOSED, NOT FROZEN** (G3 freezes)
Envelope `{ v:1, entityId, seq, ts, type, payload }`; seq 0-based gapless per A1 (harnesses emit `TimelineEventInit` WITHOUT seq; the outbox's `finalizeEvent` is the ONLY seq allocator — that split enforces A1). 15 types incl. `opaque {origin, kind, data}` (lossless foreign round-trip, deep-equal tested). `entity_spawned` always seq 0. **Delta framing = sibling `/deltas` stream** (NOT interleaved): the idempotent producer (A1/C4) can't host droppable records (a dropped fake-seq = rejected gap); deltas have no seq, `ref`→finalized event id, `idx` gaps legal, finalized event always wins on dedup, `attempt` field for T7.4 retry dedup. **Snapshot↔seq:** snapshot at seq N = state after consuming seq ≤ N (inclusive); fast-join = init from payload then consume N+1… (`checkSeqContiguity`). `archived` is episode-terminal only (resurrection continues seq). **Harness:** `run({entityId, runId, canonicalContext, wakeMessage, tools, steerSource, signal, emitDelta, commitEvents?}) → {events, stateDelta, usage}`; tool clients in `ToolContext` pre-bound to idempotency key `(entityUrl, runId, toolUseId)` via `toolIdempotencyKey`; `emitDelta` fire-and-forget invariant tested. `selectContextEvents` = summarization fold (latest wins) + context filter; `reasoning` never re-enters context. **Biggest CASDK tension:** thinking signatures are unforgeable → canonical `reasoning` cannot round-trip into a rebuilt session — resolved as deliberate display-only asymmetry (warm path unaffected), NOT via `opaque`. Naming: chose `control` event type (not PLAN's `signal`) per D8 dropped-POSIX vocab. Tests: schema 49 + harness-native 15 passed; repo lint/build/typecheck green. **G3-freeze-review items** (in casdk-mapping.md §8): (a) `control` vs `signal` name; (b) `summarization.detail` added; (c) `ContentBlock`=text+image only (future CASDK `document`→opaque); (d) `tool_result.detail` populated by in-process tool layer not stream capture (note for T7.2). Deviation: accidentally prettier-reflowed docs/self-hosting-networking.md (T1.1's untracked file; content identical). DECISIONS amendment: none.

### T2.0 — Restate semantics spike (Gate 2) — **PASSED**, code-verified (Restate 1.7.2 + TS SDK 1.16.2 in docker). Full detail in `SPIKE-RESTATE.md`.
(a) Shared handlers run concurrently with a busy exclusive handler, see mid-run K/V, and can `ctx.cancel` the in-flight wake → **interrupt/signal CAN reach a busy agent object**. But default cancellation zombies the closure + post-cancel awaits rethrow → must set `explicitCancellation:true` + race `ctx.cancellation()` + AbortController (~20ms abort). API is `@experimental` → pin SDK, conformance-test. (b) `ctx.run` results OK to 16MiB (warn ≥10MiB); ≥32MiB wedges invocation at replay; ingress cap 32MiB. Budget journal ≤~1MiB; bulk→streams (R4). (c) Idempotency-key dedups calls+sends, default retention 24h (tunable), expiry LAZY → treat retention as a floor. (d) Awakeables: `orTimeout`→catchable TimeoutError, survive endpoint restart, double/late resolve ignored. (e) Completed `ctx.run` never re-executes on replay; aborted-mid-flight re-executes from scratch (at-least-once) and zombie closures overlap retries → per-handler timeouts + `AbortSignal.any([interrupt, attemptCompletedSignal])`. (f) **A3 CONFIRMED**: `agent.<type>` accepted; keys arbitrary strings incl. url/slashes (percent-encode in ingress; reject empty key at gateway). → proposes **A4**.

### T1.1 — Compose stack
5 services on one `teaspill` network: postgres, restate, electric, durable-streams, gateway (build stub; T1.2 fills Dockerfile). Healthchecks + `restart:unless-stopped` on all EXCEPT durable-streams — **surprise: its published image `electricax/durable-streams-server-rust:0.1.4` is distroless (no shell/curl/CLI healthcheck), verified by pulling+listing fs** → healthcheck disabled, `depends_on: condition: service_started`, documented in `docs/self-hosting-networking.md §5`. Tags pinned & live-verified 2026-07-17 (pull+inspect): `restatedev/restate:1.7.2`, `postgres:17-alpine`, `electricsql/electric:1.7.7` (ships own /v1/health), `electricax/durable-streams-server-rust:0.1.4`. Postgres `wal_level=logical`, `listen_addresses=*`; Electric auto-creates publication/slot (T1.3 still owns the `entities`-table publication). **Networking stance documented** (`docs/self-hosting-networking.md`): host-run services register via `host.docker.internal`, NOT `localhost` (Restate dials deployment URLs directly — the electric loopback class); added `extra_hosts: host-gateway` to restate. `docker compose config` validated clean. Open: when T2.2 adds the durable-streams client dep, pin it to match server 0.1.4's protocol, not client-latest. DECISIONS amendment: none.

## G3 (2026-07-17)

### Gate 1 — Schema freeze review (main session) — **PASSED, schema FROZEN v1**
Reviewed `docs/casdk-mapping.md` (CASDK capture + cold-rebuild round-trip, pi-ai sketch, usage mapping) against `packages/schema/src/{events,deltas}.ts`. Round-trip lossless (warm path never projects; cold path derives 4 SESSION_FORMAT line shapes; unknowns→`opaque`; only non-round-tripped content = thinking signatures [security] + enumerated chatter). All 4 freeze-review items ACCEPTED (control-vs-signal, summarization.detail, text+image ContentBlock, tool-layer tool_result.detail). Flipped STATUS→FROZEN in events.ts/deltas.ts/casdk-mapping.md. → DECISIONS **A5**.

### T1.3 — Catalog schema + migrations
New `packages/catalog` (`@teaspill/catalog`, Drizzle ORM + postgres.js). Tables: `entities(url pk, tenant, type, status enum active|idle|archived, tags jsonb, parent, head_seq bigint?, snapshot_offset bigint?, archived_snapshot jsonb, created_at, updated_at)`, `entity_tags(url fk→entities cascade, tag, pk(url,tag))` per A2, `api_keys(id uuid, hash unique, label, created_at, revoked_at)`. **head_seq nullable, NO default** — NULL = "row exists (idempotent spawn insert) but no event confirmed yet", distinct from real seq 0 (A1). Indexes: entities(tenant/type/parent/status), entity_tags(tag). **Electric: REPLICA IDENTITY FULL required** (verified live 2026-07-17) — set on both tables in `0001_operational_setup.sql`; publication relies on Electric's auto-managed `electric_publication_default` (compose doesn't set manual publishing). Migrations: drizzle-kit `0000_init.sql` + hand-written `0001` (trigger + REPLICA IDENTITY). **DB round-trip actually ran** against `docker compose up postgres` (migrate + idempotent re-run, all constraints verified, torn down). Tests 11 pass + 4 DB-integration skipIf (green without infra). DECISIONS amendment: none (surfaced the esbuild/pnpm build-gate, fixed centrally by main — see build note).

### T2.4 — Cron object
`cron/<key>` Restate virtual object (`packages/coordination/src/cron.ts`): `schedule(spec)`/`unschedule()`/`tick()`. Cron lib **croner@10.0.1** (MIT, zero-dep, IANA tz via Intl) over cron-parser (avoids luxon). `now` read once per handler via `ctx.run("now", () => Date.now())` (SPIKE pattern, replay-safe); `nextFireAfter(afterMs, expr, tz)` pure. Each tick recomputes next-fire-after-now (no missed-tick backlog). **Generation-guard**: schedule/unschedule bump a K/V `generation`; each `tick()` carries its minting generation, no-ops on mismatch (Restate has no by-content delayed-send revoke) — test-verified an unschedule between tick-send and queued successor does NOT resurrect. DST: spring-forward NY 2026-03-08 gap=23h, fall-back 2026-11-01=25h confirmed. SDK pinned `@restatedev/restate-sdk@1.16.2`. Cron handlers stay default-exclusive (short K/V+send; A4's explicitCancellation seam is for long LLM/tool ctx.run, not here). 18/18 tests (pure/fake-context; live-runtime timing deferred to T6.3). Open: gateway-side `target.key` validation/percent-encoding (A3 empty-key footgun) is gateway's job (T1.2), cron forwards the CronTarget as given. DECISIONS amendment: none.

### Main-session central fix (during G3)
`packages/catalog` deps (drizzle-kit/tsx→esbuild) were the first with unapproved postinstall scripts → pnpm blocked install/run repo-wide, and a concurrent race briefly wrote a broken `allowBuilds` placeholder into `pnpm-workspace.yaml`. Resolved centrally: set `allowBuilds.esbuild: true` + `onlyBuiltDependencies: [esbuild]` in `pnpm-workspace.yaml`; `pnpm install` now clean (3 esbuild versions built). Verified `pnpm --filter @teaspill/catalog --filter @teaspill/coordination -r typecheck test` → all green (catalog 11+4skip, coordination 18).

## G4 (2026-07-17)

### T1.2 — Gateway service (packages/gateway) — L, CRITICAL — no HALT
**Fastify 5** + raw undici for proxying (unbuffered streaming for R5, built-in body limits, pino logs). Routes: `/api/*`→Restate ingress one-way sends (handler NAME SEAM shared with T2.1, single map in `routes/api.ts`; client Idempotency-Key forwarded; A3 enforced twice — empty-key reject + percent-encode in `ingressUrl()`); `/streams/*` **byte-exact durable-streams proxy** (raw path+query verbatim, only hop-by-hop headers stripped, responses STREAMED, client-disconnect aborts upstream); `/shapes/*` Electric proxy; `/registry/*`→Restate admin ALLOWLIST (deployments*, GET services*/health), deployment URI forwarded AS-IS (host.docker.internal stance inline, NO url rewriting). **R5 test (`src/r5-streams.test.ts`): kills a client mid-long-poll, resumes via offset THROUGH the gateway** (+ parked-poll wake, 304 revalidation, gateway-restart-mid-read, timeout-never-advances-offset) — ran against BOTH a faithful fake (ported from durable-streams-rust handlers.rs) AND the **REAL `electricax/durable-streams-server-rust:0.1.4` docker image** (18 pass/2 fake-only skip). **Auth deviation (accepted):** `sha256(key)+timingSafeEqual`, NOT bcrypt/argon2 — keys are 256-bit random (no KDF needed) and salted hashes would break T1.3's lookup-by-hash unique index; this IMPLEMENTS D6, not a contradiction. Addressing fns PORTED into gateway (schema lacks them) — TODO move to `@teaspill/schema`. Dockerfile builds (host-side `bundle` then compose build); container smoke-tested live. **45/45 tests green** (verified no-live-infra: skip-guarded). Surprise: Node IncomingMessage 'close' fires on body-consumed not disconnect → proxy abort must watch the RESPONSE ('close' + !writableEnded) or PUT/POST self-aborts. Open: confirm T2.1 handler names (done — see below); key-mint ergonomics→T6.2; CORS preflight vs auth→T1.4. DECISIONS amendment: none.

### T2.1 — Agent virtual object skeleton (packages/coordination) — L, CRITICAL — no HALT
`createAgentObject(config)` = generic `agent.<type>` Restate virtual object template (A3), `explicitCancellation:true` (A4). **T6.1 specializes purely via config** (entityType, Harness D5, tools, validators, real seam impls). Handlers: `spawn` (entity_spawned@seq0, idempotent reattach), `message` (variants: plain | child_finished | subscription_update), `signal` (**SHARED** handler), `archiveTick` (cron-style epoch guard; body deferred to T8.1 with seq/head_seq contract documented). **K/V** `{ status, seq(next-unallocated), outbox[], context[], workspaceRef, subscribers[], parentRef, usage, currentInvocationId? }` + 2 ADDITIVE keys `harness` (D5 layer-2 HarnessStateDelta) & `archiveEpoch` (D7 idle guard) — refinements, flagged. **Interrupt seam = SPIKE §a verbatim:** wake records `ctx.request().id`→K/V; shared `signal` reads it live + `ctx.cancel`→`raceInterrupt(ctx.cancellation())` aborts AbortController merged with `attemptCompletedSignal` (AbortSignal.any, A4)→catch commits control+run_finished(interrupted) durably; entity stays messageable. **T2.5 builds verbs on this.** **Seam contracts T2.2/T2.3 MUST satisfy (in `src/agent-seams.ts`):** `ProjectionOutbox.stage(ctx,entityId,inits)→TimelineEvent[]` = THE only seq allocator (A1, pure K/V, atomic under single-writer); `.flush(ctx,entityId)→{appended,headSeq}` = idempotent-producer append in ctx.run, in-order replay from first unconfirmed, confirm-then-trim, catalog head_seq. `AgentNotifier.{notifySubscribers,notifyParent}` = fire-and-forget sends. **R4:** `commitEventsChunked` stages+flushes ≤16-event slices; stub outbox enforces C4 (dup=no-op, gap=reject) in tests. Convention: handler commits wake input as message event(s) pre-run; harness gets `wakeMessage:null`. **42/42 tests green** (18 cron untouched + 23 agent + 1 index). Needs live runtime (→T6.3/T9.1): real cancel/@experimental explicitCancellation, ctx.run replay, delayed-send timing. Open: re-spawn arg-diff error needs retained args (T6.1); real child-finish semantics→T2.3/T3.3. DECISIONS amendment: none.

### Main-session integration review (G4)
Both L tasks green in isolation AND together (`pnpm --filter @teaspill/gateway --filter @teaspill/coordination -r test` → 45 + 42, no live infra needed). Handler NAME SEAM reconciled: gateway `/api/*` maps to `spawn`/`message`/`signal` — matches T2.1's handler names. T1.2's ported addressing fns + T2.1's additive K/V keys both noted as clean-up/forward items, no rework. No interface drift. Gateway auth `sha256+timingSafeEqual` deviation accepted (correct for random 256-bit keys + T1.3 hash-lookup index).

## G5 (2026-07-17)

### T2.2 — Projection outbox + idempotent append (packages/coordination) — L, CRITICAL, **Gate 3 GREEN** — no HALT
Client `@durable-streams/client@0.2.6` EXACT-pinned (pairs with the :0.1.4 server source in `../electric`; G3 open item closed). Producer semantics extracted from `handlers.rs` (validate_producer/handle_append_inner/handle_create; no PROTOCOL.md): identity `(Producer-Id, Epoch, Seq)`; **Producer-Seq is per-REQUEST not per-record**; new producer/epoch starts seq 0; same epoch seq≤last⇒204 dup no-op, last+1⇒200, gap⇒409+Producer-Expected-Seq (out-of-order-after-gap REJECTED), lower epoch⇒403 fenced; PUT-create 201/200-idempotent/409 (C3). **Surprises:** (a) client 0.2.6 `IdempotentProducer` is session-scoped and its `append()` never sends producer AppendOptions → flush does raw per-event POSTs (`Producer-Seq = seq`, the thin mapping PLAN anticipated); client used for header constants + read-back. (b) server producer-dedup state persists DEBOUNCED → SERVER crash can readmit an acked append as same-seq dup record → readers dedup by embedded seq (T5.2/T9.1). `flush`: pre-flight contiguity → one `ctx.run` appending in order from first unconfirmed (404→PUT-create once; dup=confirmed; gap/stale/closed⇒terminal `OutboxDriftError`, repair=T5.3) → trim + K/V `outboxConfirmedSeq` → `ctx.run` catalog head_seq/status upsert (drizzle GREATEST-monotonic). `stage` = sole A1 seq allocator, pure K/V, ~1MiB/event + ~1MiB pending budget (A4). **Gate 3 GREEN:** fast-check property suites (arbitrary crash/fault schedules) vs a faithful validate_producer-port fake → exactly-once/in-order/0-based-gapless/trim+catalog invariants after every recovery + dedup replay sweep; agent.ts 42 tests untouched. coordination 64/68 always-on + **4 integration ran GREEN vs real :0.1.4 docker + live compose Postgres**. `agent-seams.ts`/`agent.ts`/`agent-runtime.ts` untouched (interface satisfied; InMemory stub intact). → DECISIONS **A6**.

### T4.1 — workspace/<key> object + executor host (packages/executor) — L, CRITICAL — no HALT
`workspace` VO (key `<tenant>/<name>`, A3; `explicitCancellation:true`) = pure coordination (K/V config/status/currentInvocationId/currentExec) → `WorkspaceHostClient` seam (`createRestateHostClient` = journaled genericCall to stateless `executor-host` service; `createDirectHostClient` for tests) → host owns envs behind the **T4.2 adapter seam** `ExecutorAdapter.ensure({workspaceKey,config})→WorkspaceEnv{startExec→ExecHandle{wait,kill}, readFile/writeFile/mkdir/rm/stat/ls, dispose}`. **Long exec (SPIKE §d):** journaled awakeable → host dispatch returns immediately (dedups `(workspaceKey,execId)`, execId replay-stable from invocation id) → stdout out-of-band to per-exec stream `/t/…/workspaces/<name>/exec/<execId>/stdout` via `WorkspaceStreamSink` seam → host resolves awakeable via ingress POST → result `{execId, outcome, exitCode, tailBytes(cap 8KiB/128KiB), streamRef, durationMs}`. **Hung-command safety 3 layers:** adapter kill-tree timeout → awakeable backstop (timeout+grace, durable kill) → **SHARED `kill` handler** (live currentExec read, host kill resolves awakeable `killed`; `force:true` adds ctx.cancel + durable cleanup per A4). Shared `status` for cheap reads. **Containment (one module, ported from electric `sandbox/{path-containment,unrestricted}.ts`):** string-level for isolated adapters + realpath symlink-walk for host-FS; writes contained everywhere, reads declared per adapter. Shipped **dev-only `local` adapter** (real procs/FS — fakes can't honestly test kill-trees/symlinks). 53/53 tests. **Handler names `fsRead` not `fs.read`** (dot-in-name grammar unverified in T2.0 → gateway name-map decides public spelling; carry-forward). Open: streams-client = T2.2's 0.2.6 (seam+in-memory only here); live-runtime behaviors → T4.2/T6.3/T9.1. Refinements (not amended): exec result carries outcome/signal/durationMs beyond D4; per-exec stream granularity (addressing §4.3 left to T4.1). DECISIONS amendment: none.

### T1.4 — Optional JWT read path (packages/gateway + packages/agents-sdk) — M — no HALT
`jose@6.2.3` pinned. **Composition by token SHAPE:** a three-segment `a.b.c` bearer on a GET `/streams|/shapes` request is verified as a read token (`looksLikeJwt`); everything else (tsp_ keys, JWTs on non-read routes) takes the unchanged API-key path — neither verifier runs twice, server-side callers byte-for-byte unaffected. Read token honored ONLY on GET /streams|/shapes; on /api|/registry or non-GET → falls through to API-key → 401 (writes never bypass developer, D6). `pfx` must prefix the requested path incl. tenant (covers both `/timeline` and `/deltas` under the shared entity prefix per casdk-mapping §8.5); wrong pfx→403, expired-beyond-leeway/invalid→401 with reconnect body. Alg pinned HS256 (no alg-confusion). Leeway 60s (`GATEWAY_JWT_CLOCK_TOLERANCE_SECONDS`). **CORS** scoped to GET /streams|/shapes: OPTIONS preflight answered 204 pre-auth; CORS headers ride every GET incl. 401/403 so browsers can read status + reconnect; `GATEWAY_CORS_ALLOW_ORIGINS` (default `*`, non-credentialed bearer). Mint helper `mintReadToken({pfx,ttlSeconds,secret})` in agents-sdk (`src/read-token.ts`, rejects pfx not under /streams//shapes/). New env in gateway README: `GATEWAY_JWT_SECRET` (unset⇒disabled), tolerance, cors-origins. Tests: gateway 69 (T1.2's 45 stayed green + 24), agents-sdk 8. `.env.example` untouched. Open: token via Authorization header only (no query-param). DECISIONS amendment: none.

### Main-session integration review (G5)
All three green in isolation AND full-repo (`pnpm -r typecheck test` → 11 pkgs, 273 tests + live-infra skips). Gate 3 satisfied early (T2.2 property tests GREEN). No interface drift: T2.2 kept T2.1's seam + InMemory stub intact; T4.1's adapter seam is clean for T4.2; T1.4 composed with T1.2 auth without breaking the 45. Reconciled: durable-streams client version = 0.2.6 across all stream emitters (A6). Carry-forwards logged in resume pointer (handler-name dots, addressing-fn migration, epoch-offset).

## G6 (2026-07-17)

### T2.3 — Messaging, spawn, pub/sub (packages/coordination) — M, CRITICAL — no HALT
Real `AgentNotifier` (`createAgentNotifier`) = fire-and-forget one-way sends: notifyParent→child_finished, notifySubscribers→subscription_update, + new `send` (arbitrary agent→message); `createSendNotifier` kept as deprecated alias (T2.1 tests green). New `messaging.ts`: **spawnChild** (one-way `spawn` send to agent.<childType>/<childId> carrying parentRef, returns child_spawned init parent commits); **sendToAgent**; **notifyParentOrDeadLetter**; **scheduleSubscriberNotify/handleSubscriberNotifyTick** (debounce); **gather** (createGather/recordGatherResult). **Dead-letter = catalog status check** via new `EntityDirectory` seam (a one-way send can't fail at sender; Restate lazily creates target) → undeliverable send stages error(source=platform, code=dead_letter) on SENDER's timeline; `createDrizzleEntityDirectory` reads entities.status in ctx.run; `DEFAULT_DEAD_STATUSES=["archived"]` OVERRIDABLE (T8.1 must flip — see DECISIONS note). **Debounce** = cron generation-guard reused (bump `notifyGen`+`notifyDirty`+delayed `notifyTick({gen})`, last gen wins → one coalesced fan-out; default 250ms). New agent.ts handlers subscribe/unsubscribe/notifyTick; config gains `directory?`, `subscriberNotifyDebounceMs`. **gather-N** persists GatherState in K/V across N separate child_finished invocations, idempotent by childId. **Parallel-spawn regression (mandatory):** parent spawns N=4 in ONE wake → N spawn sends + N child_spawned → N separate child_finished invocations all delivered (seq-contiguous, no collision) + gathered (completes at Nth), redelivery never double-counts — permanent test for the upstream dropped-parent-wake bug. coordination 83 pass +4 skip. Needs live runtime→T6.3/T9.1. → DECISIONS note (dead-letter vs resurrection). Amendment: none.

### T4.2 — Adapters: local-unrestricted + docker (packages/executor) — M — no HALT
`local-unrestricted`: reuses T4.1 local impl + loud banner + REQUIRED opt-in (`allowUnrestricted` flag / `TEASPILL_ALLOW_LOCAL_UNRESTRICTED=1`, else throws). `docker`: container-per-workspace + named volume `/work` (persists across execs + container stop/remove); idle-teardown state machine (activity refcount→grace→STOP-if-persistent-else-REMOVE; new op during grace cancels+reattaches; per-env mutex) — maps onto T4.1 seam (host caches one env/key, container comes/goes beneath). Exec via `docker exec`; stdout streams through EXISTING onChunk→WorkspaceStreamSink seam (durable-streams client NOT pulled — deferred like T4.1, no version fork). Kill/timeout→in-container marker-scan `kill -KILL`→T4.1 shared-kill seam; FS-over-exec (base64, positional args). **Docker access = shell out to `docker` CLI via fakeable `DockerCli` seam, NO new deps** (dockerode rejected). **Socket-mount decision** (compose dev): root-equivalent tradeoff documented in README + code. Registry `createAdapterRegistry` selects local|local-unrestricted|docker by name. Tests: 8 lifecycle-machine (fake cli+timers) + 4 local-unrestricted + 4 registry + **9 real-container (RAN & passed** — restarted Docker Desktop; self-gates+skips if daemon absent). 78 executor tests; T4.1's 53 preserved. Open: default image alpine:3.20 (no digest pin), default bridge network egress — revisit for prod isolation. Amendment: none.

### T5.1 — Stream layout, snapshots, retention (docs/streams.md + packages/schema) — M — no HALT
**Canonical stream map** (from addressing §4): timeline (per-entity, exactly-once 0-based-gapless A6) · sibling `/deltas` (best-effort/droppable/no-seq) · workspace stdout + per-exec stdout (bulk out-of-band R4). **Snapshot cadence** (`packages/schema/src/snapshot-policy.ts`, pure/additive): AGENT OBJECT emits at outbox time (A1, never a harness); `periodic` (≥200 seq OR ≥256 KiB), `pre_archive` (forced D7), `recovery` (forced D3). Catalog `snapshot_offset` = latest snapshot seq (GREATEST-monotonic); fast-join loads snapshot@N→consume N+1 (A5). **Retention v1 = "streams grow; archive T8.1 closes per-entity; monitor disk T8.2"** — EVIDENCE (durable-streams-rust HEAD=0.1.4): **NO per-stream prefix truncation**; `--tier`/tier.rs = transparent cold-offload (length only grows); whole-stream only = `Stream-TTL`/`Stream-Expires-At`, `DELETE`→410, `--tier s3`. Policy: timeline NEVER TTL'd (D1); deltas/stdout get `Stream-TTL` (6h/24h proposed). Helper `shouldSnapshot`+`DEFAULT_SNAPSHOT_POLICY`+`selectFastJoinSnapshot` (compile-time lockstep with frozen `state_snapshot.reason`). events.ts/deltas.ts untouched. schema 65 tests. → DECISIONS **A7**. Amendment: none.

### Main-session integration review (G6)
All three green in isolation + full repo (`pnpm -r typecheck test` → 11 pkgs, ~340 tests + live-infra skips). Disjoint: T2.3→coordination, T4.2→executor, T5.1→docs+schema(additive). No drift: T2.3 satisfied T2.1's AgentNotifier seam (kept alias); T4.2 implemented T4.1's ExecutorAdapter seam (kept 53 tests); T5.1 additive to frozen schema (no event-type change). Recorded A7 (retention) + the dead-letter/resurrection follow-up for T8.1. Docker adapter's stdout still routes via the sink seam (client 0.2.6 pull deferred, consistent with T4.1) — a real stream write lands when T8.2/executor wiring needs it.

## G7 (2026-07-17) — Round 1: T3.3 ∥ T2.6 · Round 2: T2.5 (serialized to avoid agent.ts contention)

### T3.3 — Platform tools (packages/harness-native/src/tools.ts) — M — no HALT
Six tools as T3.1 `ToolDefinition`s via `platformTools()` (→ AnyToolDefinition[], toolCtx-agnostic; T3.2 + T7.2 both reuse; `platformToolsByName()`, `include`/DEFAULT_PLATFORM_TOOL_ORDER for subsetting). `spawn_agent`/`send_message` drive the pre-bound `ctx.platform` client (idempotency key `(entityUrl,runId,toolUseId)` attached BY the client, not tool code); `list_children` read-only (no key). Descriptions in `PLATFORM_TOOL_DESCRIPTIONS` TEACH the async/wake model (spawn returns id now/result later as child_finished; wait returns IMMEDIATELY, no sync blocking, agent re-woken); tests assert the teaching substrings so edits can't drop them. **GAP (flagged, not amended):** frozen `PlatformClient` has no lifecycle method + no status/terminal channel → `wait`/`finish`/`set_status` are CONTROL tools embedding a `PlatformControlSignal` in `tool_result.detail` (`PLATFORM_CONTROL_KEY`); harness reads via `readPlatformControlSignal`/`isTerminalControl` at the tool boundary (end loop for wait/finish; persist status for set_status). Own-agent state, single-writer, idempotent — safe convention. Main may later add `PlatformClient.setStatus` or a `control` field (would touch frozen interface). 39 tests (15+24). Open: PLAN budgets a description tuning pass on real transcripts.

### T2.6 — Steerbox (packages/coordination/src/steer.ts) — S/M — no HALT — ZERO agent.ts edits
`steer/<entityId>` Restate object (service `steer`, key = full entity url, A3): `push`/`drain` over a K/V queue + never-reset monotonic counter (ordering + idempotent-ish drains fall out of single-writer K/V clearing). `createHttpSteerSource` adapts to harness-native's `SteerSource.drain()` via raw ingress HTTP POST (harness drains from inside its own ctx.run, no SDK object-context — same shape as HttpTimelineStreamTransport). `decideSteerRoute` (pure): active→steer target; idle/archived/null→degrade to `message(source:"steer_degraded")` (already-frozen WakeSource, A5). **Cheap status read recommended = agent object's shared `status` handler, NOT catalog** (D1: K/V is the only control-flow store; SPIKE §a ~21ms fresh reads). `drainAtWakeStart`+`renderSteerMessagesAsEvents` helpers for the wake-start no-loss contract, with wire-in sketch documented on the function — left for T2.5 to apply. 21 new tests, 108 total. DECISIONS amendment: none.

### T2.5 — Control API: interrupt + lifecycle verbs (packages/coordination/src/control.ts + agent.ts) — M — no HALT
Four D8 verbs on T2.1's seam, each emitting a frozen `control` event. **interrupt(reason?):** SHARED front door `handleInterrupt` reads `currentInvocationId`, `ctx.cancel`s it → wake's `raceInterrupt` aborts harness (~20ms, A4) → records control(interrupt)+run_finished(interrupted), entity stays live. Caveat: shared handler can't write K/V → free-text reason returned to caller, event is verb-only (T7.2 inherits). **pause/resume:** EXCLUSIVE; `pause` sets `AGENT_KV.paused` (runtime flag, SEPARATE from frozen status enum); `handleMessage` gates at invocation start, queuing into `AGENT_KV.pausedMailbox` (no run/no events); `resume` clears + re-enqueues in order. NOT cut — sat cleanly. **archive:** EXCLUSIVE `applyArchive` minimal-correct (flush → status=archived → control(archive)+state_snapshot(pre_archive) → terminal archived(snapshotSeq) → clear ALL K/V); same body backs the `archive` verb AND the idle `archiveTick` (**idle auto-archive now FIRES** — was a stub). Resurrection NOT built (T8.1); catalog `archived_snapshot` JSONB deferred to T8.1 (needs @teaspill/catalog writer seam; snapshot STATE is on the stream). **Steerbox wake-start drain wired** (`drainAtWakeStart(r.steerSource)` prepended to runWake preEvents — T2.6's no-loss contract). +11 tests, 115 pass +4 skip. → DECISIONS **A8**. **Open ⚠:** idle auto-archive strands entities until T8.1 (resurrection + dead-status flip) — keep it opt-in/off by default until then.

### Main-session integration review (G7)
All green in isolation + full repo (`pnpm -r typecheck test` → 11 pkgs, ~393 tests + skips). Serialization worked: T2.6 made ZERO agent.ts edits, T2.5 then owned agent.ts edits cleanly (interrupt/pause/resume/archive handlers + steer wake-start drain). No drift. Recorded A8. Two carry-forwards: (1) idle auto-archive must stay off until T8.1; (2) tool-interface lifecycle channel gap (T3.3) — decide at T6.1/T7.2.

## G8 (2026-07-17) — T3.2 cut off by session limit mid-task, FINISHED on resume

### T3.2 — pi-ai harness (fully step-durable loop) · packages/harness-native — M/L — no HALT
pi-ai: `@mariozechner/pi-ai@0.73.1` (pinned exact; electric validated 0.70.x, API identical). Install side effect: pnpm-workspace.yaml `allowBuilds` false for `@google/genai`+`protobufjs` (pi-ai transitive, build scripts unneeded). **Loop (`pi-harness.ts`):** every LLM call its own `ctx.run` (`pi:llm-<n>`; journaled = bounded assistant turn, bulk text via emitDelta only); every tool call its own journaled step (`pi:tool-<s>-<i>`) through injected ToolContext bound to `toolIdempotencyKey(entityUrl,runId,toolUseId)`; canonical events commit through `commitEvents` (outbox seam, handler allocates seq/A1) at EVERY step boundary; no naked clock/random between steps. **Steer:** journaled drain before EVERY LLM step → committed as user `message` events + injected. CHOICE: a steer does NOT abort an in-flight generation (aborting a ctx.run → Restate re-runs+re-bills on retry; boundaries seconds away); the `interrupt` verb still aborts immediately (frozen contract). **Summarization:** budget = cache-inclusive usage anchor + pure estimate of appended tail (pi-adapter pattern) → over budget → summary via its OWN `ctx.run` → `summarization` event, `replacesThroughSeq` = last context-bearing CANONICAL seq (mid-run events have no seq yet, A1); 1 fold/run (a 2nd needs commitEvents to return seqs). **Provider errors (`pi-client.ts`):** TIMEOUT/UNREACHABLE/RATE_LIMITED/UNAVAILABLE = retryable → rethrown (Restate retries that step only); AUTH/INVALID_REQUEST/unknown = terminal → journaled as step result → error(source:'provider')+run_finished('error'); completed/terminal steps never re-billed. **Seams** `HarnessCtx`/`PiStepClient`/`ToolContextFactory` all injected → loop tests offline; real client `pi-provider.ts` (per-provider `buffered` flag silences deltas only, journal granularity identical; BUFFERED_PROVIDERS empty pending soak). `pi-context.ts` pure assembly + dangling-tool-call repair + **images preserved in user/tool_result content** (the flagged fix; assistant blocks are text-only — provider has no assistant image type). **59 new tests** (pi-harness 23, pi-context 11, pi-client 19, pi-provider 6): §7 ordering, idempotency key, steer injection, summarization, terminal-vs-retryable, replay-no-re-bill, interrupt, control tools, deltas, image regression. harness-native 119/119. **Frozen-interface GAPS (noted, not edited → T6.1):** (1) run-boundary authorship overlap — harness `emitRunBoundaries` vs agent.ts runWake both author run_started/finished; step-durable wiring picks ONE. (2) HarnessRunInput has no `contextTokens` seed (first-step budget uses estimate). (3) `wake.source` defaults "message" when wakeMessage null (handler knows real source). Amendment: none.

### T4.3 — Agent tool bindings (packages/harness-native/src/workspace-tools.ts) — M — no HALT
Registry `workspaceTools()` (mirrors `platformTools()`): `bash`, `read_file`, `write_file`, `edit_file`, `ls` + `workspaceToolsByName`/`DEFAULT_WORKSPACE_TOOL_ORDER`/`WORKSPACE_TOOL_NAMES`/`include`. **edit_file unique-match-or-fail COPIED from electric `edit.ts`** (strict single-match, no replace_all): 0→isError "not found"; >1→isError "not unique, add more context"+count; exactly 1→replace+write; docstring teaches extending old_string. Effects route through `ctx.workspace.*` (WorkspaceClient pre-bound to idempotency key); exec/writeFile keyed, read_file/ls read-only, edit_file is read-modify-write (only terminal writeFile keyed). **`ToolContext.workspace?: WorkspaceClient` confirmed present** (interface.ts, optional per D4) covering exec/readFile/writeFile/ls/mkdir/rm/stat — NO interface edit needed. No-workspace → non-fatal isError. 21 tests (registry + bash + read/write/ls + edit_file all 3 match cases). No new deps. **GAPS (→T3.2/T7.2 wiring):** (a) WorkspaceClient.exec has no AbortSignal → `ctx.signal` not forwarded to long exec (abort via interrupt verb/workspace object); (b) ls(path) required not optional (tool defaults to "."). index.ts export applied by main (T4.3 avoided the contended file). Amendment: none.

### T5.3 — Drift reconciler + repair (packages/coordination/src/reconciler.ts) — M — no HALT
`reconciler/<partition>` cron-style self-rescheduling object (cron.ts generation-guard). Each tick SAMPLES a bounded catalog batch via round-robin cursor in its own K/V (intervalMs=60s/batchSize=50 default). **Cheap confirmed-seq read (A6#4):** shared-read probe of entity K/V `outboxConfirmedSeq`+pending depth — NEVER a stream scan; no-drift tick = 1 catalog read + 1 probe/entity, zero stream I/O. Three drift classes (`classifyDrift`, pure): `catalog_lag` (head_seq<confirmed→GREATEST re-upsert via T2.2 OutboxCatalog) · `stuck_outbox` (pending>0→re-drive flush, idempotent) · `unrecoverable` (flush→OutboxDriftError→recovery snapshot request + `AlertSink` seam for T8.2). History-hole = frozen `state_snapshot(recovery, historyHole:true)` (A5), no new event type. **A6#6 RESOLVED → A9:** auto repairs never bump epoch (Producer-Seq==seq holds); catastrophic reset designed as affine `Producer-Seq = seq − offset` gated by `allowEpochReset` (default false); the epoch-bumping append lives in projection-outbox.ts (off-limits) → reconciler detects+alerts+requests only. 24 new tests, coordination 139/143. → DECISIONS **A9**. Amendment: A9 (proposed, accepted).

### Main-session integration review (G8)
T3.2's early cutoff handled: its code was in-tree + typechecking; resumed the same agent via SendMessage to apply the image-block fix + write the 59-test suite → 119/119. T4.3 ran in parallel scoped to a NEW file only (returned its index export line, applied by main) — no clobber of the concurrent T3.2 edits. Applied `export * from "./workspace-tools.js"`; removed a dead `usageZero()` (lint). Full repo: 0 TS errors, ~493 tests green, lint 0 errors. Recorded A9. Four carry-forwards routed to T6.1/T7.2/T8.1 (see resume pointer action items) — the harness↔agent-object wiring seams (run boundaries, wake.source, contextTokens, tool lifecycle channel, exec AbortSignal) all land at T6.1/T7.2.

## G9 (2026-07-17) — T6.1 first agent aborted (0 work), re-dispatched fresh

### T6.1 — defineAgent + registration (packages/agents-sdk) — M, CRITICAL — no HALT
`defineAgent({type,revision?,spawnSchema,inboxSchemas,state,harness,tools?,onWake?,baseline?})` → `AgentDefinition` with `.compileConfig(deps)`/`.compile(deps)` (→ createAgentObject) + `.registration()` (revisioned manifest). Harness seam: `native(cfg)`/`claudeAgentSdk(cfg)` → `HarnessSpec.finalize(userTools)` → `HarnessSelection`. `native` compiles to the NEW additive `AgentObjectConfig.buildHarness` (per-wake `createPiHarness` bound to the live ctx as `HarnessCtx`; platform T3.3 + workspace T4.3 + user tools; HTTP-ingress `ToolContext` with idempotency key, injectable+lazy client). `claudeAgentSdk` = typed stub: compiles, no buildHarness, `.run()` throws "CASDK harness not yet available (T7.1)". **G8 gaps RESOLVED:** (a) harness authors run_started/run_finished (`emitRunBoundaries`) + `runWake` step-durable branch stops authoring them — static path BYTE-IDENTICAL; (b) `wake.source` threaded via new pi-harness `wakeSource` opt; (c) prior `usage.contextTokens` seeded via new pi-harness `initialContextTokens` opt; (d) pi-harness already reads `readPlatformControlSignal` at tool boundary — `runStepDurableWake` captures `finish` result from committed run_finished.detail → parent child_finished. **Coordination edits (minimal/additive):** agent.ts `buildHarness?` + `HarnessBuildContext` + `runStepDurableWake` + `armInterruptAbort` in adaptExclusive; agent-runtime.ts optional `armInterruptAbort?()`. **pi-harness.ts (non-frozen) additive:** 3 optional default-preserving opts (frozen interface.ts/schema UNTOUCHED — no HALT needed). `serve()`/`registerDeployment()` (gateway /registry/deployments; T6.2 owns retry/backoff). Revision: additive-only guard (`diffStateSchema`/`assertStateRevision`) enforced at defineAgent time. Tests: agents-sdk 29 (8 T1.4 + 21), coordination 139, harness-native 119 — all green. Open→T6.2: `onWake` reserved (not loop-wired); listChildren/workspace tool clients are deployment seams; real ingress/interrupt need live runtime (T6.3). Open→T7.1: `claudeAgentSdk` descriptor is the seam to replace. Amendment: none.

### T5.2 — Frontend SDK (packages/frontend-sdk) — L — no HALT
**Conformance test WRITTEN FIRST** (`reducer.conformance.test.ts`): mid-stream join (snapshot@15 + 16..24 ≡ full-replay-from-0 on fold position/entityState/window), A6 duplicate idempotency (each event twice ≡ once), finalized-wins delta interleave, gap⇒drift vs duplicate⇒NOT-drift vs historyHole⇒sanctioned-jump, out-of-snapshot (missing promised snapshot ⇒ loud `missing_join_snapshot`). Fixtures via frozen `finalizeEvent` (A5), reusable by T6.3. **Reducer** (pure): collections {runs, messages, toolCalls, reasoning, children, controls, errors, summarizations, snapshots, opaques, liveDeltas, liveUsage} + entityState/archived/spawned. Rules: (1) A6 seq-idempotent (`seq<=appliedThroughSeq` dropped/counted/never drift); (2) A7 fast-join (skip <N, init snapshot@N — accepts seq>N since offset is a floor A6#5, consume N+1…); (3) **finalized always wins** (deltas.ts contract): per-ref idx-ordered chunk merge, finalized message/reasoning/tool_call/run_finished supersedes + late chunks drop; run_finished sweeps that run's stragglers (rule ADDED — wire test caught the cross-session hole); attempt supersede per T7.4. **driftDetected** + onDrift callback; only `state_snapshot(historyHole:true)` jumps drift-free. `createAgentTimeline(streamUrl,{fromSnapshot,auth,deltas,live,onDrift})` uses `@durable-streams/client@0.2.6` through gateway `/streams/*`; per-request auth header fn (token refresh across long-polls). `useAgentCatalog`/`createAgentCatalog` uses `@electric-sql/client@1.5.24` (new dep, server 1.7.7) via `/shapes/v1/shape`, scalar filters + raw-where escape. `actions` spawn/send/control → `/api/*`, Idempotency-Key passthrough, writes never JWT (D6). **`@durable-streams/state`: VENDORED THE PATTERN, not the package** (it materializes electric's StateEvent change-records + TanStack DB = the stream-as-entity-truth model D8 dropped; teaspill folds A5 domain events natively). Core has ZERO react imports; hooks behind `./react` (react optional peer). 51 tests +1 live. **Live smoke vs REAL `electricax/durable-streams-server-rust:0.1.4` docker PASSED** (skip-guarded): PUT-create, per-event producer POSTs (Producer-Seq=seq), duplicate re-POST⇒204, full replay + fast-join from a real byte offset. Open: (a) fast-join needs a stream BYTE offset — catalog stores only snapshot seq → T2.2/T8.1 persist byte offset (else read scans from 0, correct not cheap); (b) addressing helpers duplicated in gateway+actions.ts → promote to @teaspill/schema; (c) finalizedRefs grows per session (string ids, fine, documented). Amendment: none.

### Main-session integration review (G9)
T6.1's first agent aborted (0 tool uses, no writes) — re-dispatched fresh, succeeded. Disjoint: T6.1→agents-sdk (+minimal additive coordination/pi-harness edits for the run-boundary fix), T5.2→frontend-sdk. Full repo: 0 TS errors, ~564 tests green, lint clean. All four G8 harness-wiring carry-forwards resolved at T6.1 (run boundaries, wake.source, contextTokens, tool lifecycle). New carry-forwards routed: T6.2 (register retry, real ingress clients, listChildren, onWake wiring), T8.1 (byte-offset persistence for cheap fast-join), schema cleanup (addressing fns). No drift; no amendment.
