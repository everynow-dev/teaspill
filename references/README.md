# references/

Scratch space for repos an agent self-clones for spike/research work (e.g. `../electric`, a Restate SDK checkout). Contents are gitignored — nothing here is committed.

Any durable finding belongs in the relevant plan's `notes/` dir (`work/plans/NNNN-<slug>/notes/`), not here. This directory is throwaway.
