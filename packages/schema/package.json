{
  "name": "@teaspill/schema",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical event schema, entity addressing, and shared types.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "ulidx": "^2.4.1",
    "zod": "^4.4.3"
  }
}
