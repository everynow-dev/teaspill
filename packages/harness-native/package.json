{
  "name": "@teaspill/harness-native",
  "version": "0.1.0",
  "private": true,
  "description": "pi-ai native harness — the step-durable, owned agent loop.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@teaspill/schema": "workspace:*",
    "zod": "^4.4.3"
  }
}
