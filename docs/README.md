# docs/

The documentation now lives in the public docs site under [`packages/docs`](../packages/docs).

- **Read it online:** https://teaspill.everynow.dev
- **Run it locally:** `pnpm --filter @teaspill/docs dev`

Design history and internal decision records live under [`work/`](../work) — start at [`work/README.md`](../work/README.md) — and in the per-package `README.md` files.

The previous `docs/*.md` guides (addressing, agents-sdk, auth, backup-restore, frontend-sdk, schema-reference, self-hosting, streams) were migrated into the site; their content now lives on the site pages above, and the original files remain in git history.
