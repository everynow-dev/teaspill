# 0002 — Worklog

Append-only findings ledger. One entry per completed task: what was built, deviations, surprises, open questions. Read before starting dependent work.

---

## ▶ RESUME POINTER (main session — update after every group)

- **Status:** G5 done + committed (T3.2 · T4.1; both agents resumed from in-session context across a ~75-min pause, completed clean, all green). Next: dispatch **G6** (T3.3 observability, solo).
- **Note — plan 0003 now active concurrently** (`work/plans/0003-docs-site/`, packages/docs — another session). Plan-level conflict rule: 0003 owns `packages/docs` (+ AGENTS.md/INDEX.md self-registration, which are NOT 0002's to commit — leave them for the 0003 session). No package overlap with 0002's remaining tasks. Before each 0002 dispatch, re-check 0003's resume pointer.
- **Gates status:** Gate 1 — GREEN. Gate 2 (T4.2 live conformance) — pending; **T4.1 deployment now exists to run it against**.
- **⚠ Carry-forwards for T4.2 (G7) — T4.1 flagged 3 live-seam gaps (findings, not worked around):** (a) **per-entity steer drain unwirable** — `AgentObjectConfig.steerSource` is one `SteerSource` per TYPE but `createHttpSteerSource` needs an `entityId`; reference leaves steer unwired → live steer push/drain (on T4.2's list) will only degrade to wakes until an additive per-wake steerSource-factory seam exists. (b) **`emitDelta` entityId gap** — `DeltaInit` carries no entityId, config-level emitter can't stamp it → deltas left no-op. (c) **spawn `workspaceRef` unreachable** — gateway `/api/spawn` accepts no workspaceRef and `HarnessBuildContext` doesn't expose K/V workspaceRef → tools always use the derived private workspace key. T4.2 must confirm these live or land additive seams.
- **Follow-up candidates surfaced (not scheduled):** agents-sdk zod pin (4.1.13) vs schema/harness-native (^4.4.3) — dual-version tsc OOM landmine for anyone consuming defineAgent; align agents-sdk to ^4.4.3. agents-sdk `serve()` has no reconciler binding + no custom `validateMessage`/per-entity `steerSource` (ergonomics). `entities.parent` is written by NO platform writer (only the reference tool client now sets it at spawn) — candidate promotion into coordination's child upsert. These fit a future plan or T5.4 notes.
- **To continue in a fresh session:** read PLAN.md + this pointer + `git log`, check INDEX.md (0003 active — package-overlap rule), dispatch G6.
- **Gates status:** Gate 1 — **GREEN**, `allowEpochReset` default flipped true. Gate 2 (T4.2 live conformance) — pending (needs T4.1 deployment first).
- **Open amendments:** **0002:A1** (T1.4) — Restate handler names may not contain a dot; camelCase FS handlers permanent. **0002:A2** (T2.1) — resurrection carries producer epoch/offset (refines 0001:A10's "epoch stays 0" clause). Both binding.
- **Carry-forwards:** (T1.1) schema has no first-party unit test for `addressing.ts` — behavior covered via gateway (72) + frontend-sdk (51) tests; candidate for a future coverage task. (T1.1) schema `TYPE_RE`/`ID_RE` length caps (48/64) now apply to frontend-sdk `entityApiPath` — a spec-correct tightening, no existing test exercised overlong segs. (T1.3) `FakeStreamsServer` stays in conformance (materially different fake w/ restart/dedupBySeq/rawRecords); only the `validateProducer` protocol fn was promoted.
- **To continue in a fresh session:** read `work/plans/0002-follow-ups/PLAN.md` + this pointer + `git log --oneline`, check `work/INDEX.md` for other active plans (package-overlap rule), then dispatch the next group. Assign models by S/M/L tier via the dispatch profile in `work/README.md`.

---

## G1 — Debt & seam cleanups (committed)

### T1.1 — Promote addressing helpers to `@teaspill/schema`

Moved the canonical addressing derivation functions (docs/addressing.md §9) into a new `packages/schema/src/addressing.ts`, exported from `packages/schema/src/index.ts`. Ported the FULL reference implementation (not just the subset gateway had), including `AddressingError`, `entityUrl`/`parseEntityUrl`/`isEntityUrl`, `toHttpForm`/`fromHttpForm`, `newInstanceId`/`assertInstanceId`, stream-path derivations, workspace-key derivation, and the Restate key mapping (`restateAgentKey`/`steerKey`/`restateWorkspaceKey`/`restateCronKey`/`timelineProducerId`), plus all constants/regexes.

- `packages/gateway/src/addressing.ts`: gutted to a thin re-export of the names gateway consumers use. Kept the file (not deleted) because `addressing.test.ts`, `ingress.ts`, `r5-streams.test.ts` import `"./addressing.js"` by relative path. Added `@teaspill/schema: workspace:*` to gateway package.json (was not a dep before).
- `packages/frontend-sdk/src/actions.ts`: replaced local `SHORT_FORM_RE`/`CANONICAL_RE`/`SEG_RE` with schema's `SHORT_FORM_RE`/`ENTITY_URL_RE`/`TYPE_RE`/`ID_RE`. Already depended on schema.
- Added `ulidx: ^2.4.1` to schema package.json (needed by `newInstanceId`).
- Did NOT touch events.ts/deltas.ts (frozen-surface rule; verified via git status).

Deviation: schema's `TYPE_RE`/`ID_RE` impose length caps (48/64, per addressing.md §2.3) that frontend-sdk's old `SEG_RE` did not — a behavior tightening (spec-correct), no test exercises overlong segs, both suites green with zero assertion changes. Open: no first-party schema unit test for addressing.ts (see resume pointer carry-forward).
Verify: schema typecheck/test (65)/build/lint, gateway typecheck/test (72)/lint, frontend-sdk typecheck/test (51)/lint — all PASS. Root `pnpm lint` PASS.

### T1.2 — `packages/agents-sdk` README

Wrote `packages/agents-sdk/README.md` (previously the only package without one). Covers: `defineAgent` (fields, validation, `.compile`/`.compileConfig`/`.registration()`), harness selection (`native`/`claudeAgentSdk`, shared tool-context wiring, CASDK lazy-load), the `onWake` contract (quotes 0001:A10 — hand-off vs onWake-only, resurrection tie-in), registration (`serve`/`registerDeployment`, one-attempt-throws owned by `teaspill dev`, `CompileDeps` seams), revision rules (additive-only, `StateRevisionError`, `diffStateSchema`/`assertStateRevision`), plus `mintReadToken` and coordination re-exports (`createDrizzleArchiveCatalog`, `ArchiveCatalog`), and a layout table. Design-note tone matching schema/harness-native READMEs; deep content linked to `docs/agents-sdk.md` by anchor, not duplicated. No surprises vs docs — export surface matches the guide exactly.

### T1.3 — Export coordination test utilities

Exported `FakeTimelineServer` and `validateProducer` from `@teaspill/coordination` under a new `./testing` subpath export → `dist/testing/fake-timeline-server.{js,d.ts}` (kept off the main entry `src/index.ts` — no accidental prod dep; build already emitted the artifact via `tsconfig.build.json` include, no build-config change). Deleted the line-for-line `validateProducer` re-port from conformance's `fake-streams.ts`; it now imports the canonical fn from `@teaspill/coordination/testing`, and conformance `index.ts` re-exports it from there. `FakeStreamsServer` stays in conformance (materially different fake — restart/dedupBySeq/rawRecords chaos needs; only the protocol fn was in scope). Workspace dep already present in conformance + chaos. Chaos never imported `validateProducer` directly (only `FakeStreamsServer` via conformance) — zero chaos source changes.
Verify: coordination build/typecheck/test (171 pass, 4 skip)/lint, conformance build/typecheck/test (17 pass, 5 skip)/lint, chaos typecheck/test (16 pass, 5 skip)/lint — all PASS.

---

## G2 — name-map verdict, fast-join plumb, docker hardening (committed)

### T1.4 — Handler-name grammar: dotted names settled (dots ILLEGAL in handler names)

Added ephemeral probe `references/restate-spike/src/dotted-handler-probe.ts` (spike dir gitignored — not committed; verdict is the durable artifact) and ran it against live Restate 1.7.2 (`operea-restate-1`, ingress :8080 / admin :9070, ports discovered via `docker port`, not assumed). **Verdict: dotted HANDLER names ILLEGAL, dotted SERVICE names LEGAL.** SDK constructs `fs.read` silently but admin `POST /deployments` discovery rejects it (HTTP 500, pattern `^([a-zA-Z]|_[a-zA-Z0-9])[a-zA-Z0-9_]*$`); control probe registered dotted service `agent.researcher` at HTTP 201 → `agent.<type>` scheme confirmed sound (closes addressing.md §10.5). camelCase FS handlers (`fsRead`… ) BLESSED permanent; NO handler renamed (none needed). Documented: `docs/addressing.md` §6.1 (new, with verbatim evidence) + §10.5 marked RESOLVED. Authoritative public→internal name-map = `AGENT_HANDLERS` in `packages/gateway/src/routes/api.ts` (confirmed single, no second inline map); gateway/executor source untouched (read-only inspection). Both probe deployments deregistered — live Restate returned to prior state. Amendment **0002:A1** recorded. Non-blocking follow-up for T5.4 docs sweep: comment at `packages/executor/src/workspace.ts:30-34` still says dots "not verified" — can now cite T1.4.

### T1.5 — Fast-join byte offset: plumb `snapshot_stream_offset` to the reader

Catalog column `snapshot_stream_offset` (opaque durable-streams BYTE offset, TEXT, from 0001:T8.1) was never surfaced by frontend-sdk, so `createAgentTimeline` scanned from 0. Distinct from `snapshot_offset` (bigint canonical **seq**, already surfaced). Surfaced byte offset as `EntityRow.snapshotStreamOffset: string | null` via new `toOpaqueOffset()` (preserves TEXT token, never arithmetic; empty ⇒ null). New exported `fromSnapshotForRow(row)`: no seq ⇒ undefined (full replay); byte offset NULL ⇒ `{seq}` only (reader scans "-1", reducer resolves at seq floor); both present ⇒ `{seq, offset}` (cheap seek). Reader already consumed `opts.fromSnapshot?.offset ?? "-1"` — only population was missing, no reader change. NULL fallback preserved 3 places. Conformance (reducer.conformance.test.ts): +2 e2e scenarios (byte offset landing BEFORE snapshot still resolves via seq floor `skippedPreJoin===3`; NULL offset falls back to scan-from-0). Live smoke (timeline.live.test.ts, skip-guarded on `TEASPILL_LIVE_STREAMS_URL`) extended with early-offset assertion against real :0.1.4. Reducer early-offset tolerance (0001:A6#5/A7) was already implemented; this closed the catalog→timeline plumbing gap. Docs note: frontend-sdk.md example still hand-wires `fromSnapshot` — adopt `fromSnapshotForRow` in T5.4 sweep. No @teaspill/catalog change (column already exposed). Verify: frontend-sdk typecheck/test (57 pass, 1 live skip)/lint PASS.

### T5.2 — Docker adapter prod hardening

Closed 0001:T4.2's 3 open items; adapter INTERFACE unchanged (config + defaults only). (1) `DEFAULT_DOCKER_IMAGE` now digest-pinned `alpine:3.20@sha256:d9e853e87e55526f6b2917df91a2115c36dd7c696a35be12163d44e6e2a4b6bc` (multi-arch index digest, captured 2026-07-18 from daemon; refresh recipe inline). (2) New `DockerNetworkMode = "none"|"bridge"|(string&{})` per-workspace + adapter-level `network` option → `docker run --network`; **default `"bridge"`** (documented — agents need egress for tool calls; `"none"` for untrusted code). (3) Socket-mount prod tradeoff documented (not built; no DinD) — "Production hardening" section in docker-adapter.ts module doc + docs/self-hosting.md block making explicit that digest-pin+network harden *workspaces* NOT the socket-mounted executor (multi-tenant hostile-code still needs rootless DinD / VM adapter). +6 tests in docker-lifecycle.test.ts (fake captures full create spec). Verify: executor typecheck/test (89, incl. 9 real-container ran)/lint PASS.

---

## G3 — affine offset + reconcile handlers (Gate 1), key minting CLI (committed)

### T2.1 — Affine offset append + agent-object reconcile handlers — L, CRITICAL, **Gate 1 GREEN**

Implemented the 0001:A9 epoch-reset path. **Affine append** (projection-outbox.ts): `Producer-Seq = canonicalSeq − outboxProducerSeqOffset`, offset in new K/V `OUTBOX_KV.producerSeqOffset` beside `producerEpoch` (both absent ⇒ 0 ⇒ identity, A1 byte-identical; all prior tests passed unmodified). New `offset_underflow` pre-flight guard; `OutboxDriftError` now carries structured `detail` (`pending_gap|confirmed_mismatch|offset_underflow|producer_gap|stale_epoch(serverEpoch)|bad_epoch_start|closed`). **Reset step** (`handleReconcileRecovery`, the ONLY writer of epoch/offset): re-verify drift with a live flush (spurious ⇒ healthy no-op) → fold stuck pending into bounded context → size-bound snapshot BEFORE any destructive write → drop pending → stage `state_snapshot(recovery, historyHole:true)` at next canonical seq N → `epoch = max(stored, serverEpoch)+1`, `offset = N`, `confirmedSeq = N−1`, clear stale byte-offset → flush ⇒ snapshot appends at Producer-Seq 0 (404 ⇒ PUT-create). **Canonical seq never resets** (A1); only the producer coordinate resets. **Handlers** (agent.ts): `reconcileProbe` SHARED (4 K/V gets, no ctx.run/I/O — never blocks behind a busy wake), `reconcileFlush`/`reconcileRecovery` EXCLUSIVE; `createRestateEntityReconcileClient` names resolve. **A9 split kept exactly**: reconciler detects/alerts/requests, agent object executes, reconciler never touches outbox K/V (asserted byte-for-byte). **Gate flipped**: `allowEpochReset` default → `true` (after property suite covered reset); execution-time re-verify guard + per-spec opt-out preserve A9's "ops opts in" as opt-OUT. **Property suite** (projection-outbox.test.ts): Gate-3 suite extended with catastrophe schedules (arbitrary stream-loss / producer-rollback / append-fault / crash interleavings at every recovery journal boundary), asserting after every settled step: reader seq-dedup (byte-identical, first wins ⇒ **epoch/offset provably invisible to readers, A6#2**), in-order, every hole bridged by a historyHole snapshot, exactly-once content per seq, 0-based gapless allocation, K/V settled, server producer state == (K/V epoch, head−offset). Deterministic crash-point sweep over the recovery's journal boundaries + reconciler→handlers e2e (gate open: alert+reset+heal+ok ticks; gate closed: alert+recovery_gated+untouched).

**Surprises (A9-level rigor):** (1) property suite CAUGHT A REAL BUG — journaled clock read sat between pending-drop and snapshot-stage, stranding an unmarked hole on from-scratch re-execution; fixed by moving the clock read before the destructive block (drop→stage→reset journal-boundary-free). (2) Resurrection previously hard-reset `producerEpoch` to 0 (correct pre-reset per A10) — a post-reset entity that archived+resurrected would be fenced forever; fixed by carrying epoch/offset through `ArchiveSnapshotState` (additive optional, absent ⇒ 0) → **amendment 0002:A2**. (3) Snapshot catalog upsert previously skipped when byte offset unknown, but the recovery snapshot's SEQ is what fast-join needs (A6#5 floor) — flush now upserts seq always, byte offset only when known. No schema change (`reason:"recovery"` + `historyHole` already in frozen v1).

**Adversarial review round (2 findings, both fixed before commit):** (1) BLOCK — a snapshot rejected by the outbox's OWN budgets (`maxEventBytes`/`maxPendingBytes`, knobs independent of `archiveSnapshotMaxBytes`, no ordering enforced) threw `OutboxBudgetError` AFTER the destructive drop, committing an unmarked hole through a terminal (non-crash) path the FakeCtx crash model couldn't reach. Fixed: drop→stage restores pre-drop K/V exactly on any stage failure (context via clear-if-null for byte-faithfulness, outbox back to pending; no journal boundary ⇒ atomic with the drop), then rethrows to the `failed` path; `failed` now guarantees pending-intact-for-retry. Regression test provokes the misconfigured-limits case (snapshot passes archive bound but exceeds outbox event budget) and asserts byte-for-byte K/V restoration + subsequent full recovery under sane limits. (2) Closed-stream recovery previously performed a futile reset per reconciler tick (fresh snapshot + epoch bump forever, unbounded canonical-seq/epoch inflation — churn, not corruption). Fixed: `detail.kind === "closed"` ⇒ new `{performed:false, reason:"stream-closed"}` alert-and-hold BEFORE any destructive step; repeated ticks are pure no-ops (asserted). Reconciler-side cosmetic: tick still records action `recovery_snapshot` for a held closed stream — candidate polish for T2.2. Also documented FakeCtx write model as deliberately pessimistic (immediate commits surviving simulated crashes; NOT Restate buffered-write/journal-replay — consistency under it is a fortiori).

Verified myself: restore block (projection-outbox.ts:930-960) captures `pending`/`existingRaw` pre-drop (896-897), restores byte-faithfully on stage throw, no journal boundary between — atomic. Tests **171→187** pass / 4 skip. Property suite re-run 8×+5× post-fix (~1600 randomized reset-boundary schedules), stable. Verify: coordination build/typecheck/test (187 pass, 4 skip)/lint + full `pnpm -r typecheck`/`pnpm lint` PASS. Files: projection-outbox.ts, agent.ts, reconciler.ts, archive-snapshot.ts, control.ts, projection-outbox.test.ts, README.md. Design note in coordination README.

### T5.1 — API key minting ergonomics

Added `teaspill keys create|revoke|ls` — OPERATOR-context CLI talking to catalog Postgres directly (NOT a gateway route; gateway has no admin-auth tier — documented in help + docs/auth.md). Canonical key primitives moved into `@teaspill/catalog` (`src/keys.ts`): `newApiKey()` (`tsp_` + 32 random bytes base64url = 256 bits) + `hashApiKey()` (sha256 hex), byte-identical to gateway's — pinned by a fixed sha256 vector. DB connection via existing injected-`CliDeps` pattern (new `KeysStore` seam + `createKeysStore(databaseUrl)`, default-wired to catalog's client; tests use `fakeKeysStore`, no live DB). Revoke selector: plaintext token (hashed+matched), full uuid, or key-id prefix (row stores no token prefix — deliberately no key-prefix mode). Security: plaintext minted in memory, printed ONCE by create, never persisted/logged — Postgres holds only sha256 hash (asserted). Revoke = soft delete (`revoked_at`), idempotent. Tests: catalog +13, cli +13. Verify: cli typecheck/test (55)/lint, catalog typecheck/test (21 pass, 8 DB skip)/lint PASS.

### Gateway auth consolidation (fold of T5.1's discovered debt — main session, not deferred)

T5.1 surfaced that `gateway/src/auth.ts` carried a byte-identical `newApiKey`/`hashApiKey` copy (couldn't be touched from T5.1's scope). Rather than defer, the main session consolidated it in this group: `gateway/src/auth.ts` now imports+re-exports both from `@teaspill/catalog` (dep already present) — ONE canonical impl, no duplicate. Public surface (`gateway/src/index.ts`) + `auth.test.ts` unchanged (still re-exported). Updated the now-stale note in `catalog/src/keys.ts`. Verify: gateway typecheck/test (72)/lint, catalog test (21) PASS. Same debt class as T1.1 (addressing) — caught + closed same-cycle.

---

## G4 — reconciler live loop, exec abort, backup regression (committed)

### T2.2 — Reconciler live loop + alert wiring

Wired the reconciler for real deployment (post-Gate-1). **Client wiring:** `createRestateEntityReconcileClient` default handler names match agent.ts's bound handlers (probe SHARED, flush/recovery EXCLUSIVE); probe path stays a genericCall to the 4-K/V-get shared handler, no ctx.run — cheap, never blocks behind a busy wake. **Scheduling:** added `ReconcilerScheduleClient` + `createHttpReconcilerScheduleClient` (raw Restate-ingress POST to `/reconciler/<partition>/{start,stop}`, mirrors steer.ts) + `scheduleReconcilers({client,enabled,partitions?,spec?,logger?})` — the compose-adjacent D4 bootstrap hook **T4.1 must call after serve+register** (correct home: reconciler object is deployment-side, CLI talks to gateway not ingress). Generation-guarded (start bumps object generation atomically with spec, cron.ts discipline ⇒ stale tick = no-op, idempotent by supersession). Unit-test safe: `createReconcilerObject` inert, `scheduleReconcilers` a logged no-op unless `enabled:true`. **Alert fan-out:** `createReconcilerAlertSink({metrics?,logger?})` — unrecoverable drift now fans out from ONE place to `recordDrift` (0001:T8.2 counter, tagged `entity.type`) + WARN operator log; moved `recordDrift` out of `reconcileEntity` into the sink so metric+log can't disagree; `ReconcilerAlert.entityType` added (additive). **Closed-stream fix (T2.1 follow-up closed):** `driveRecovery` now returns the real `ReconcileRecoveryResult` (was void); new `recoveryActionFor()` maps it to accurate `RepairAction` — `stream-closed`→`recovery_held` (no longer claims a `recovery_snapshot` it never wrote), plus `recovery_gated`/`recovery_healed`/`recovery_failed`. e2e suite now returns the real handler result → mapping exercised end-to-end; closed-stream case asserts `recovery_held` + byte-for-byte K/V untouched across repeated ticks. **Live test:** reconciler.live.test.ts skip-guarded on `TEASPILL_RECON_INGRESS_URL` (scheduling seam: start→fresh generation, re-start supersedes, stop tears down). Full "induce lag+stuck outbox, watch repair" needs live agent objects to create drift ⇒ owned by T4.2/T4.3 with T4.1 deployment; repair LOGIC covered e2e over fakes here. No A9/A10 contradiction (detect/alert/request vs execute split preserved); no schema/frozen-interface change. Verify: coordination build/typecheck/test (187→196 pass, 6 skip)/lint + full workspace typecheck/lint PASS.

### T3.1 — Exec abort: AbortSignal through WorkspaceClient.exec

Closed 0001:T4.3/T7.2 gap (`ctx.signal` couldn't reach a long exec). **Additive frozen-surface change (confirmed strictly additive/default-preserving):** optional `signal?: AbortSignal` on `ExecOptions` in `harness-native/src/interface.ts` — absent ⇒ byte-identical to pre-0002; WorkspaceClient/ToolContext/Harness signatures untouched. **Ingress-boundary contract (documented on exec + the field):** the signal does NOT travel to the process — on abort the client fires the workspace `kill` handler for THIS exec's `execId`, driving the existing 3-layer kill path (adapter kill-tree → awakeable `killed` → killed-outcome). Client-side kill-on-abort, not in-process signal magic. **Plumbing:** `bashTool` forwards `ctx.signal` as `ExecOptions.signal` (both harnesses benefit via shared registry). **Reusable helper:** `linkExecAbortToKill` in `executor/src/exec-abort.ts` (exported) — transport-agnostic abort→kill glue that **T4.1's concrete ingress WorkspaceClient will consume** (supplying a stable execId + a `kill` callback that ingress-calls `workspace.kill`). Idempotent (inherits 0001:T4.1 kill safety): kill fires ≤once; already-aborted⇒immediate kill+skip dispatch; abort-after-completion⇒dispose no-op; double-abort⇒single fire; kill errors swallowed (awakeable-timeout backstop bounds a wedged exec). Scope note: concrete ingress client doesn't exist yet (0001:T6.2 seam, T4.1's job) — T3.1 delivers field+doc+bash-forward+helper; abort→kill goes live e2e in T4.2/T4.3. Tests: executor exec-abort.test.ts (8 cases) + harness-native workspace-tools (+1). Verify: harness-native typecheck/test (120)/lint, executor typecheck/test (97)/lint PASS.

### T5.3 — Backup lossy-combo regression

Added the automated regression 0001:T8.3 left open: asserts docs/backup-restore.md §4.2 (restore catalog+streams WITHOUT Restate ⇒ never-archived ACTIVE entity lost via loud TerminalError; ARCHIVED entity resurrects). 6th conformance scenario `BACKUP_LOSSY_RESTORE` (mode `live`), LIVE + chaos-tier gated (BOTH `TEASPILL_CHAOS=1` AND `TEASPILL_STACK_URL` — chaos/env.ts discipline, inlined since conformance can't import chaos). **Exact error string** confirmed against agent.ts handleMessage (~1360): full string `agent <url> has no live state (not spawned, or archived with no resurrectable snapshot)` — pinned as `noLiveStateTerminalError(url)` constant, byte-asserted offline. **Script-drives** backup.sh/restore.sh via execFileSync using only the two scripts (empty-Restate baseline backup + full backup, then restore full postgres+streams + empty restate). **Surprises (load-bearing, documented):** (1) gateway `send` is one-way (202) so the server-side TerminalError can't travel back — active-entity-lost asserted via observable proxy (no post-restore reply in bounded window) + the exact-string constant; (2) wiping Restate also wipes the deployment registration (metadata in restate-data volume) ⇒ re-registration is a REQUIRED recovery step, exposed as `TEASPILL_BACKUP_REREGISTER_CMD`; (3) added `observeStateUntil` helper since live.ts didn't cover the `archived` view. Live path not exercised (per task) — deliverable is the skip-guarded regression. Verify: conformance typecheck/test (22 pass, 6 skip incl. new one skipping cleanly)/lint + chaos still green PASS.

---

## G5 — commitEvents multi-fold, reference deployment (committed)

*(G5 was interrupted at the session limit and resumed ~75 min later: both subagents were stopped via TaskStop, a one-shot in-session cron re-fired, and both were resumed from their transcripts via SendMessage — context survived intact. No re-dispatch needed.)*

### T3.2 — `commitEvents` returns seqs; multi-fold summarization

Lifted 0001:T3.2's one-fold-per-run cap. **Additive seam (frozen surface):** `HarnessRunInput.commitEvents` return widened `Promise<void>` → `Promise<readonly TimelineEvent[]>` (harness-native/interface.ts) — default-preserving (callers ignoring the value still compile; repo-wide typecheck confirms incl. harness-casdk, which never provides the seam). The finalized events already existed internally (`commitEventsChunked`/`outbox.stage`); agent.ts `runStepDurableWake`'s closure now returns the `committed` it already computed. No schema/delta change. **Multi-fold (pi-harness.ts):** fold boundary is now a mutable `foldBoundarySeq` seeded from `latestContextBearingSeq` and ADVANCED inside the commit helper from the seam's returned events (context-bearing ev.seq > boundary ⇒ boundary = ev.seq). Each fold sets `replacesThroughSeq = foldBoundarySeq`; the summarization event is itself context-bearing so the next fold covers the prior fold's summary + intervening steps. Progress guard (`foldBoundarySeq !== lastFoldBoundary`) makes repeated folds meaningful AND auto-caps no-commit callers at one fold. `maxSummarizationsPerRun` default 1 → `POSITIVE_INFINITY` (self-limiting via progress guard + shrinking budget + maxSteps). **A4 journal budget:** harness never retains committed event bodies — extracts the last context-bearing seq (a number) and lets events go out of scope. Test: a run over budget TWICE folds twice (replacesThroughSeq 1 then 6, covering fold1's summarization at seq 3), contiguous seq alloc [2..9], `selectContextEvents` keeps only fold2's summary + trailing message (latest-wins). Surprises: (1) closure-mutated `foldBoundarySeq` needs an explicit `const foldSeq: number` since TS won't narrow it at the fold site; (2) coordination typechecks against harness-native's built `dist/*.d.ts` — rebuild harness-native after the interface change. Verify: harness-native typecheck/test (121)/lint, coordination typecheck/test (196 pass, 6 skip), full `pnpm -r typecheck` (all 12) PASS.

### T4.1 — Reference deployment: example agents + compose overlay

**Location decision: `packages/reference-deployment` (`@teaspill/reference-deployment`), NOT `examples/`** — it's infrastructure the acceptance suites depend on (T4.2/T4.3 run against it; chaos default service names now name its containers), and stays inside existing workspace/vitest/eslint globs untouched. Written as the getting-started example (README = the guide); all seams wired from public package APIs.

**Agent-loop service:** compiles the 4 deterministic onWake-only conformance agents (NO LLM, per 0001:T6.3): `conformance-echo` (spawn-respond + crash-resume + projection-continuity subject), `conformance-fanout-parent`/`-child` (parallel-fanout: N children in ONE wake via `wake.spawn`, deterministic runId-derived ids), `conformance-long-exec` (workspace-exec-durability: real exec through the ingress WorkspaceClient inside `wake.ctx.run`, idempotency key `toolIdempotencyKey(entityUrl, runId, "on-wake-exec")` so an agent-loop restart re-ATTACHES to the same invocation — the D4 invariant the scenario asserts). Local `onWakeOnlyHarness()` satisfies defineAgent without a provider. Env-gated demos `demo-pi` (native, ANTHROPIC_API_KEY) + `demo-casdk` (claudeAgentSdk, +TEASPILL_DEMO_CASDK=1, SDK external to the bundle) — missing prereq ⇒ agent not served, logged, never crashes. Real seams: DurableStreamsProjectionOutbox over HttpTimelineStreamTransport, Drizzle catalogs (resurrection works), idempotent migrations on boot, reconciler bound via `createCoordinationEndpoint({agents, reconciler})`.

**Bootstrap order** (unit-tested): listen → gateway-health wait (CLI `waitForHealthy` reused) → `registerDeployment` via gateway `/registry/*` with `retryWithBackoff` (0001:T0.4 honored — no raw Restate registration) → **`scheduleReconcilers(...)`** (T2.2 hook, exactly as demanded) via `createHttpReconcilerScheduleClient`, retried. `teaspill dev --deployment` also re-registers; both idempotent (`force:true` / generation supersession).

**Executor-host service:** co-located workspace + executor-host (createDirectHostClient), docker adapter (T5.2-hardened defaults) over mounted socket, ingress awakeable resolver, new best-effort `WorkspaceStreamSink` (closes the executor "real sink deferred" note; never throws/blocks).

**0001:T6.2 seams now real:** `createIngressWorkspaceClient` (per-op derived idempotency keys `<toolKey>#w<n>`, deterministic sha-256 exec ids, memoized ensure-on-first-use w/ retry, **`linkExecAbortToKill` consumed** — all 4 abort→kill cases); `createReferenceToolContext` (wraps agents-sdk httpToolContext, per-tool workspace client bound to `privateWorkspaceKey(entityUrl)`); **real `listChildren`** over a drizzle ChildrenStore (`WHERE parent=$1` on `entities_parent_idx`).

**Surprises (platform gaps closed deployment-side, 0001-level):** (1) **`entities.parent` was written by NO platform writer** — column+index existed since 0001:T1.3 but no `upsertHead` set it (parentRef lived only in K/V/archive JSONB), so catalog `listChildren` had nothing to read. Reference tool client now records linkage at spawn in the same journaled step (idempotent set-once upsert touching ONLY `parent`; no conflict with the child's single-writer flush; best-effort). Candidate promotion into coordination. (2) **Loose send bodies die at the frozen schema** — gateway forwards `{text}`/`{command}` verbatim but `AgentMessageInput` wants `content: ContentBlock[]` and finalizeEvent zod-parses ⇒ wake would throw (would've broken T4.2 scenario 1 on first contact). Fixed via the documented `validateMessage` NORMALIZE seam (`normalizeLooseMessage` layered by `compileLooseConfig`). (3) **zod dual-version tsc OOM** — agents-sdk pins zod 4.1.13, schema/harness-native ^4.4.3; a consumer on 4.4.x passing schemas into defineAgent's 4.1.13 ZodType params OOMs tsc (>4GB). Landmine for every future consumer — pinned 4.1.13 + README warning here; align agents-sdk (follow-up). (4) **cli bin auto-run hijacks bundles** — cli index self-runs when `import.meta.url===argv[1]`, true for any esbuild bundle inlining it. Fixed additively: cli gains side-effect-free `./register` subpath export (+ `createHealthProbe`/`BackoffOptions` exports). (5) latent pre-existing break: cli.test.ts `EntityRow` fixture predated T1.5's `snapshotStreamOffset` (masked by stale dist) — one-line additive fixture fix.

**Chaos placeholder fill (0001:T9.1 closed):** chaos `DEFAULT_SERVICE_NAMES` `agent-loop`/`executor` now name the overlay's real compose services (doc-only in chaos/env.ts + README; no chaos logic changed).

**Out-of-package edits (all additive, reviewed by main session):** chaos/{env.ts,README.md} (doc), cli/{index.ts,package.json} (additive `./register` subpath + exports), cli/cli.test.ts (T1.5 fixture fix), pnpm-lock (reference-deployment deps only — no docs deps). **Excluded from this commit** (owned by concurrent plan 0003): AGENTS.md, work/INDEX.md, work/plans/0003-docs-site/.

**T4.2 flags + follow-ups:** recorded in the resume pointer above. No amendment (the flags are seam gaps, not decision conflicts). Compose overlay networking = case (a) service-name URLs on the `teaspill` network; host.docker.internal host-run recipe documented in-file. Both docker images build clean (in-build bundle smoke); merged compose config validates. Full overlay `up` NOT run here (host ports occupied) — live bring-up is T4.2's gate. Verify: reference-deployment typecheck/test (42 pass, 1 live skip)/lint; full `pnpm -r typecheck` (14 pkgs), `pnpm -r test` (863 pass/28 skip), `pnpm lint` PASS.
